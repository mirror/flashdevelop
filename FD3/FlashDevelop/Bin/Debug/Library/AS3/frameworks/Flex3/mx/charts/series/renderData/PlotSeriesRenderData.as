/**********************************************************/
/*** Generated using Asapire [brainy 2008-Mar-07 11:06] ***/
/**********************************************************/
package mx.charts.series.renderData {
	import mx.charts.chartClasses.RenderData;
	public class PlotSeriesRenderData extends RenderData {
		/**
		 * The radius of the individual PlotSeriesItem objects.
		 */
		public var radius:Number;
		/**
		 * Constructor.
		 *
		 * @param cache             <Array (default = null)> The list of PlotSeriesItem objects representing the items in the dataProvider.
		 * @param filteredCache     <Array (default = null)> The list of PlotSeriesItem objects representing the items in the dataProvider that remain after filtering.
		 * @param radius            <Number (default = 0)> The radius of the individual PlotSeriesItem objects.
		 */
		public function PlotSeriesRenderData(cache:Array = null, filteredCache:Array = null, radius:Number = 0);
	}
}
