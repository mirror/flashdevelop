/**********************************************************/
/*** Generated using Asapire [brainy 2008-Mar-07 11:06] ***/
/**********************************************************/
package mx.charts.series.renderData {
	import mx.charts.chartClasses.RenderData;
	public class BubbleSeriesRenderData extends RenderData {
		/**
		 * Constructor
		 *
		 * @param cache             <Array (default = null)> The list of BubbleSeriesItem objects representing the items in the dataProvider.
		 * @param filteredCache     <Array (default = null)> The list of BubbleSeriesItem objects representing the items in the dataProvider that remain after filtering.
		 */
		public function BubbleSeriesRenderData(cache:Array = null, filteredCache:Array = null);
	}
}
